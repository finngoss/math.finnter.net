Consider an operator $A$, that is an object defined over a function space that maps functions to functions.
$$A:f\to Af$$
But should we care about operators simply as maps on $L^2$ or other sufficiently nice spaces? Or how else might we come to know such objects?

The simplest operator is just the identity:
$$I:f\to f$$
Though we might instead choose to write this as
$$f(x)=\int_{-\infty}^\infty \delta(x-y)f(y)dy$$
This

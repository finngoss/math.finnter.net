The kernel of an operator is defined 


The kernel of an operator shows up in the integral transform.

For example the identity $I:f\to f$ can be written as
$$If(x)=\int_{\R} \delta(x-y)f(y)dy$$ where $\delta$ is the [[Dirac delta]] function.